import { CountryValidatorDirective } from './country-validator.directive';

describe('CountryValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
